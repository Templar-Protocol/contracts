name: Release Artifacts

# Cuts a contract's canonical WASM — the only way a release blob is produced.
# Invoked per version being shipped, via `just release-wasm <tag>`: build in the
# pinned NEP-330 image at that tag's commit, upload it to the GitHub Release, and
# hand the catalog row — tag, asset, digest, length — to `catalog-pr.yml`.
# Why the build happens at the tag, why it is not fired by the tag, and why the
# record lands a commit later are in "Contract WASM artifacts" in RELEASING.md.

on:
  # Dispatch from the default branch and name the tag, rather than running at the
  # tag: GitHub reads this file from the ref it is dispatched against, so
  # `--ref <tag>` would run whatever copy of it that tag happens to carry.
  workflow_dispatch:
    inputs:
      tag:
        description: Release tag to build and publish the WASM for
        required: true

permissions:
  contents: read

env:
  CARGO_TERM_COLOR: always
  SQLX_OFFLINE: "true"

# Per tag, and never cancelled: two dispatches for one tag can both clear the
# asset-existence check and collide on upload, but cancelling the first could
# interrupt one mid-flight. The loser queues and replays against identical bytes.
concurrency:
  group: release-artifact-${{ inputs.tag }}
  cancel-in-progress: false

jobs:
  release-artifact:
    name: Build and publish the release WASM
    runs-on: ubuntu-latest
    timeout-minutes: 60
    permissions:
      contents: write # attach assets to the GitHub Release
    steps:
      - name: Checkout the tag
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
        with:
          ref: ${{ inputs.tag }}
          # The reproducible build reads committed git state.
          fetch-depth: 0
          persist-credentials: false

      # Not `./.github/actions/rust`: the WASM build runs inside the cargo-near
      # Docker image, so a host rust-cache entry for this job would be cold on
      # every invocation and just add eviction pressure.
      - uses: dtolnay/rust-toolchain@dd44c20b1206a46e25fba8503d5d7c9a33bd355a
        with:
          toolchain: "1.86.0"
          components: rustfmt

      - name: Resolve the artifact from the tag
        id: catalog
        env:
          REF_NAME: ${{ inputs.tag }}
        run: |
          set -euo pipefail
          # Exit 2 = not a catalogued NEAR artifact (a Soroban tag, say);
          # 1 = a genuine failure. A skip must not look like a broken lookup.
          #
          # clap also exits 2 on a usage error, and this file is read from the
          # default branch while the checkout is the tag — so a tag whose
          # `prebuild-test-contracts` predates `--resolve` would otherwise skip
          # silently and green a release with no WASM. Demand the sentinel that
          # only the skip path prints.
          status=0
          resolved=$(cargo run --quiet -p templar-contract-artifacts \
            --features workspace-loader,clap --bin prebuild-test-contracts -- \
            --resolve "$REF_NAME") || status=$?

          if [ "$status" -eq 2 ] && [ "$resolved" = "skip=not-catalogued" ]; then
            echo "::notice::${REF_NAME} has no catalogued WASM artifact; nothing to build."
            exit 0
          elif [ "$status" -ne 0 ]; then
            echo "::error::${REF_NAME} cannot be resolved; it may predate the \
          \`--resolve\` flag, in which case build it from a checkout of the tag." >&2
            exit "$status"
          fi

          {
            echo "$resolved"
            echo "build=true"
          } >> "$GITHUB_OUTPUT"

      - name: Install cargo-near
        if: steps.catalog.outputs.build == 'true'
        uses: ./.github/actions/cargo-near

      - name: Build reproducibly at the tag
        id: build
        if: steps.catalog.outputs.build == 'true'
        env:
          SOURCE_PATH: ${{ steps.catalog.outputs.source_path }}
          TARGET: ${{ steps.catalog.outputs.target }}
          VERSION: ${{ steps.catalog.outputs.version }}
          ASSET: ${{ steps.catalog.outputs.asset }}
        run: |
          set -euo pipefail
          cargo near build reproducible-wasm --manifest-path "${SOURCE_PATH}/Cargo.toml"

          built="target/near/${TARGET}/${TARGET}.wasm"
          mkdir -p dist
          cp "$built" "dist/${ASSET}"
          (cd dist && sha256sum ./*.wasm > checksums.txt)

          # From checksums.txt, so the digest pinned in the catalog is by
          # construction the one published on the Release.
          sha=$(cut -d' ' -f1 < dist/checksums.txt)
          length=$(wc -c < "dist/${ASSET}")
          {
            echo "sha256=${sha}"
            echo "length=${length}"
          } >> "$GITHUB_OUTPUT"
          echo "Built ${TARGET} ${VERSION} at $(git rev-parse HEAD): ${sha} (${length} bytes)"

      - name: Attach the WASM to the release
        if: steps.catalog.outputs.build == 'true'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REF_NAME: ${{ inputs.tag }}
        run: |
          set -euo pipefail
          # release-plz creates the tag and the GitHub Release moments apart, so a
          # dispatch made straight after a release PR merges can arrive between
          # the two. Wait rather than failing a build that succeeded.
          published=false
          for attempt in 1 2 3 4 5; do
            if gh release view "$REF_NAME" >/dev/null 2>&1; then
              published=true
              break
            fi
            echo "Release $REF_NAME not published yet (attempt ${attempt}); waiting."
            sleep 15
          done
          if [ "$published" != true ]; then
            echo "::error::Release $REF_NAME never appeared; cannot attach the WASM." >&2
            exit 1
          fi

          # Never `--clobber`: the catalog pins the digest of whatever was
          # uploaded first, so replacing an asset strands every fetch on a hash
          # that no longer exists. Identical bytes replay; different bytes fail.
          #
          # Compare *every* asset before uploading any. Interleaving the two
          # would let an upload land before a later file's conflict is found —
          # `dist/*` visits checksums.txt first, so a divergent WASM would be
          # detected only after its checksum had already been published, leaving
          # the release describing bytes it does not serve.
          existing=$(mktemp -d)
          missing=()
          for file in dist/*; do
            name=$(basename "$file")
            if ! gh release download "$REF_NAME" --pattern "$name" --dir "$existing" 2>/dev/null; then
              missing+=("$file")
              continue
            fi
            if ! cmp -s "$file" "$existing/$name"; then
              echo "::error::${name} is already published with different bytes. \
          Released assets are immutable; cut a new version rather than replacing one." >&2
              exit 1
            fi
            echo "::notice::${name} is already published with identical bytes."
          done

          for file in "${missing[@]+"${missing[@]}"}"; do
            gh release upload "$REF_NAME" "$file"
          done

      # The hash and length are taken from the bytes this job just built, so the
      # catalog entry attests the CI build output rather than a re-download.
      - name: Write the catalog row
        id: row
        if: steps.catalog.outputs.build == 'true'
        env:
          # The catalog key, and so the name of the file `record-release` writes.
          ARTIFACT: ${{ steps.catalog.outputs.artifact }}
          VERSION: ${{ steps.catalog.outputs.version }}
          ASSET: ${{ steps.catalog.outputs.asset }}
          SHA256: ${{ steps.build.outputs.sha256 }}
          LENGTH: ${{ steps.build.outputs.length }}
          REF_NAME: ${{ inputs.tag }}
        run: |
          set -euo pipefail
          # Same feature set as the catalog lookup above: a different one is a
          # different compilation unit, and this job has no rust-cache, so it
          # would recompile from cold rather than relink.
          cargo run --quiet -p templar-contract-artifacts \
            --features workspace-loader,clap --bin record-release -- \
            "$ARTIFACT" "$VERSION" "$REF_NAME" "$ASSET" "$SHA256" "$LENGTH"

          # build.rs validates the row, but only at compile time — so compile
          # here, where the failure names the release that caused it.
          cargo build --quiet -p templar-contract-artifacts

          echo "path=contract/artifacts/releases/${ARTIFACT}@${VERSION}.tsv" >> "$GITHUB_OUTPUT"

      # Handed over rather than committed here: a release batch tags several
      # contracts at once, and `catalog-pr.yml` records whatever rows are
      # pending as one commit on one PR. It trusts rows only from successful
      # runs of this workflow, so this is the only job that can mint one.
      - name: Hand the row to the catalog job
        if: steps.catalog.outputs.build == 'true'
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          # One fixed name, so the catalog job can list every pending row in a
          # single query; the file inside carries which release it records.
          name: catalog-row
          path: ${{ steps.row.outputs.path }}
          if-no-files-found: error
          # The window in which a broken catalog job can be fixed and re-run
          # without the release having to be recorded by hand.
          retention-days: 7
