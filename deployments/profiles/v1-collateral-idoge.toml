# DOGE, held through NEAR Intents (`idoge`), as the collateral asset of a v1
# market that deploys its own proxy oracle.
#
# `idoge-ixlmusdc` is the only v1 market holding it today, and this profile
# does not describe it: that market still reads the Pyth oracle, which is
# being retired in favor of Lazer. Adopting this profile is the migration.
[collateral]
asset = "nep245:intents.near:nep141:doge.omft.near"
symbol = "DOGE"
reference = { coin_gecko = { id = "dogecoin" } }
decimals = 8
aggregator = "median_low"
min_sources = 1
sources = [
    { kind = "lazer", oracle = "pyth-lazer.v1.tmplr.near", feed_id = 13, weight = 8 },
    { kind = "red_stone", oracle = "redstone-adapter.v1.tmplr.near", price_id = "DOGE", weight = 2 },
]
