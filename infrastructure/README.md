# Infrastructure

Infrastructure-as-code for deployment environments.

- `gcp/`: OpenTofu configuration for GCP CI/CD + runtime (Artifact Registry, GitHub OIDC, relayer MIG, funding-bridge VM, market-monitor VM, accumulator VM; no liquidator)
